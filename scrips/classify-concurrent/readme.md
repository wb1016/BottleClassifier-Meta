# image dataset classification script
this script classifies the images as three categories: colorless bottle without label sticker, colorless bottle with label sticker, something else.
this is bit different than classify/classify_images.py because it has concurrent job feature for faster work.
requires vision language model and Python 3.
